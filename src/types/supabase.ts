export interface Service {
  id: number;
  title: string;
  description: string;
  icon_name: string;
}

export interface Process {
  id: number;
  title: string;
  description: string;
  order: number;
}

export interface ValueProposition {
  id: number;
  title: string;
  description: string;
  icon_name: string;
}